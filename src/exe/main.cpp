#include <sfp/samfileparser++.h>

int main(int argc, char* argv[]) {
	/*if (argc < 2) {
		std::cerr << "Usage: " << argv[0] << " <input.sam>" << std::endl;
		return 1;
	}
	std::string inputFile = argv[1];
	sfp::SamFileParser parser(inputFile);
	try {
		parser.parse();
	} catch (const std::exception& e) {
		std::cerr << "Error parsing SAM file: " << e.what() << std::endl;
		return 1;
	}
	return 0;*/
}
