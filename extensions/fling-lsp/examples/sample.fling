let x = 10;
const y = 20;

fn add(a, b) {
    a + b
}

if x < y {
    print("x ist kleiner als y", x, y);
} else {
    print("x ist groesser oder gleich y");
}

let i = 0;
while i < 3 {
    print("i =", i);
    i = i + 1;
}

let liste = [1, 2, 3,];
print(liste.length);

let obj = { name: "Fling", version: 1 };
print(obj.name);

let ergebnis = add(x, y);
print(ergebnis);

let bad = "a" + "b";
y = 5;
let z = obj[unknownVar];
foo().bar;
if x > 0 { } else if y > 0 { }
let dup = 1;
let dup = 2;
print(w);
