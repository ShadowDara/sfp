r:
	TASK build


run:
	RUNWIN ./out/build/x64-Debug/

build:
  RUN cmake -S . -B build
  RUN cmake --build build


# Run build Task
b:
  TASK build


# Generated the buildin samfile
g:
  RUN python build.py
